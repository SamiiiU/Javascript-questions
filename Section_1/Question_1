// Question: Reverse a string without using the built-in reverse() method.

const strin = "reverse string";
let rev_string = '';

for(i= strin.length - 1; i >=0 ; i--){
    rev_string += strin[i];
}

console.log(rev_string);

